// You are given two numbers, stored in a variable with the following names one, two. If both the value are even, print "Both", else print "No".
one=4; two=7;
if(one%2==0&&two%2==0)
{
    console.log("Both");
}
else
{
    console.log("No");
}